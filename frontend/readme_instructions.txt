run "npm istall" --> in CLI (It will install all dependency and devDependencies listed in 'package.json' file)

before running the project

to run the project use "npm run dev" in CLI on ./frontend directory